version: 0.0.{build}
branches:
  only:
    - msvc_static
skip_tags: true
max_jobs: 1
image: Visual Studio 2017
clone_folder: c:\projects\libtgvoip
shallow_clone: true
cache:
  - ../opus
  - ../openssl
configuration: Release
platform: x86
build_script:
  - sh build_scripts/01-openssl.sh
  - cmd: build_scripts/02-opus.bat
  - sh build_scripts/03-prepare-build-deps.sh
